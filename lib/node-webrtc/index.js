var webrtc = {};

webrtc.PeerConnection = function(configuration, signalingCallback){
 //Tbd. impl just started. 
 console.log("not implemented yet.");
};


for ( var key in webrtc) {
  exports[key] = webrtc[key];
}